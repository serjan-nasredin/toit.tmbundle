{
  "cmd": ["toit", "parser", "validate", "--color=false", "--confdir=/tmp", "--vardir=/tmp", "$file"],
  "working_dir": "${prject_path:${folder:${file_path}}}",
  "selector": "source.toit",
  "path": "opt/local/bin:$PATH",
  "windows":
  {
    "cmd": ["toit.bat", "parser", "validate", "--color=false", "$file"]
  }
}
